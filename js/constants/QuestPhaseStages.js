const QuestPhaseStages = Object.freeze({
    PARTICIPATION: "PARTICIPATION",
    SELECT_QUEST: "SELECT_QUEST",
    PREPARATION: "PREPARATION",
    RESOLUTION: "RESOLUTION"
});

export default QuestPhaseStages;
